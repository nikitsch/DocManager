export enum RoutesPaths {
  ALL = '*',
  ARCHIVE = 'archive',
  CREATE = 'create',
  DEFAULT_PATH = '/',
  ID = ':id',
  LOGIN = 'login',
  REGISTER = 'register',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}
